function  문자출력함수(문자) {
    console.log(문자)
};

문자출력함수('PC지키미');

// printChar = 문자출력함수 //printchar에다 함수 할당
// printChar('mirim')

printChar = function (문자){
    console.log(문자)
};

printChar('청소 꼼꼼히');